{% extends 'Ansi\GhassenBundle::layout.html.twig' %}

{% block Lastmsg %}
    <div class="navbar-custom-menu">
                        <ul class="nav navbar-nav">
                            <li class="dropdown messages-menu">
                <a href="{{ path('message_last') }}" class="dropdown-toggle" data-toggle="dropdown">
                  <i class="fa fa-envelope-o"></i>
                  <section id="notification"></section></a>
                  <ul class="dropdown-menu"><li class="header">Last messages</li>
                      <div>{% block Lastmsg %}{% endblock %}</div></ul>
                  
                </li>
 <li class="dropdown messages-menu">
                <a href="{{ path('message_last') }}" class="dropdown-toggle" data-toggle="dropdown">
                  <i class="fa fa-envelope-o"></i>
                  <section id="notification"></section></a>
                  <ul class="dropdown-menu"><li class="header">Last messages</li>
                    
                  
                  {% for message in msg %}
                  <li>
                    <!-- inner menu: contains the actual data -->
                    <ul class="menu">
                      <li><!-- start message -->
                        <a href="#">
                          <div class="pull-left">
                            <img src="{{ asset(message.sender.photo) }}"  class="img-circle" alt="User Image"/>
                          </div>
                          <h4>
                            {{message.sender.username }}
                            
                          </h4>
                          <p>{{message.message}}</p>
                        </a>
                      </li><!-- end message -->
                 
                  
                 </ul>  
                  </li>{% endfor %}
                    </ul>
                  
                </li> 

{% endblock %} 
